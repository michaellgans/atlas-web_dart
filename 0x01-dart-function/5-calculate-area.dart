// Task 5 - Calculate Area

double calculateArea(double height, double base) {
  String area = (.5 * base * height).toStringAsFixed(2);
  double calculatedArea = double.parse(area);
  return calculatedArea;
}
